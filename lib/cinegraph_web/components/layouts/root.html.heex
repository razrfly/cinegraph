<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable]">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title default="Cinegraph" suffix=" · Cinegraph">
      {assigns[:page_title]}
    </.live_title>
    <%= if assigns[:meta_description] do %>
      <meta name="description" content={assigns[:meta_description]} />
    <% end %>
    <%= if assigns[:canonical_url] do %>
      <link rel="canonical" href={assigns[:canonical_url]} />
    <% end %>
    <!-- Open Graph Meta Tags -->
    <%= if assigns[:og_title] do %>
      <meta property="og:title" content={assigns[:og_title]} />
    <% end %>
    <%= if assigns[:og_description] do %>
      <meta property="og:description" content={assigns[:og_description]} />
    <% end %>
    <%= if assigns[:og_image] do %>
      <meta property="og:image" content={assigns[:og_image]} />
    <% end %>
    <%= if assigns[:og_type] do %>
      <meta property="og:type" content={assigns[:og_type]} />
    <% end %>
    <%= if assigns[:og_url] do %>
      <meta property="og:url" content={assigns[:og_url]} />
    <% end %>
    <meta property="og:site_name" content="Cinegraph" />
    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <%= if assigns[:og_title] do %>
      <meta name="twitter:title" content={assigns[:og_title]} />
    <% end %>
    <%= if assigns[:og_description] do %>
      <meta name="twitter:description" content={assigns[:og_description]} />
    <% end %>
    <%= if assigns[:og_image] do %>
      <meta name="twitter:image" content={assigns[:og_image]} />
    <% end %>
    <!-- JSON-LD Structured Data -->
    <%= if assigns[:json_ld] do %>
      <script type="application/ld+json">
        <%= Phoenix.HTML.raw(Jason.encode!(assigns[:json_ld])) %>
      </script>
    <% end %>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/alpinejs@3.13.5/dist/cdn.min.js"
      integrity="sha384-BxpSbjbDhVKwnC1UfcjsNEuMuxg4af5IXOaSi1Iq5rASQ/9a7uslhEXbP9UI/fXo"
      crossorigin="anonymous"
    >
    </script>
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="bg-white">
    <header class="px-4 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-7xl">
        <div class="flex items-center justify-between border-b border-zinc-100 py-3">
          <div class="flex items-center gap-6">
            <a href="/" class="text-lg font-bold text-zinc-900">
              Cinegraph
            </a>
            <nav class="flex items-center gap-4 text-sm font-medium">
              <.link navigate={~p"/movies"} class="text-zinc-700 hover:text-zinc-900">
                Movies
              </.link>
              <.link navigate={~p"/lists"} class="text-zinc-700 hover:text-zinc-900">
                Lists
              </.link>
              <.link navigate={~p"/awards"} class="text-zinc-700 hover:text-zinc-900">
                Awards
              </.link>
              <.link navigate={~p"/people"} class="text-zinc-700 hover:text-zinc-900">
                People
              </.link>
              <.link navigate={~p"/movies/discover"} class="text-zinc-700 hover:text-zinc-900">
                Discovery
              </.link>
            </nav>
          </div>
        </div>
      </div>
    </header>
    {@inner_content}
  </body>
</html>
